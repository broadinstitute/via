{
  "id": "variant-interpretation",
  "version": "1.0.0",
  "name": "variant-interpretation",
  "description": "Variant interpretation app: React + TypeScript frontend served by a Java + Spring Boot backend on port 8080.",
  "options": {
    "cloud": {
      "type": "string",
      "enum": ["gcp", "aws"],
      "default": "gcp",
      "description": "Cloud provider (gcp or aws)"
    },
    "login": {
      "type": "string",
      "description": "Whether to log in to workbench CLI",
      "proposals": ["true", "false"],
      "default": "true"
    }
  }
}
